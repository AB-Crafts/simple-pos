# Base URL of the backend REST API (see ../backend).
# Copy this file to .env and adjust if your backend runs elsewhere.
# The app works fully offline with no backend running at all — this is
# only used for background sync when a connection is available.
VITE_API_URL=http://localhost:4000/api
